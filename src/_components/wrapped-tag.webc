<template webc:root><slot></slot></template>

<style webc:scoped>
  :host {
    display: inline-block;
    font-size: 0.75rem;
    text-transform: uppercase;
    border: 1px solid var(--color-gray-lighten-30);
    color: var(--color-gray-base);
    border-radius: 2rem;
    font-weight: 600;
    padding: 0.125rem 0.5rem;
    line-height: 1;
  }
</style>
